import org.joda.time.{DateTime, Period}
package recfun

object Worksheet {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(119); 
  println("Welcome to the Scala worksheet");$skip(27); 


 val d = DateTime.now();System.out.println("""d  : <error> = """ + $show(d ))}
 
 
 
}
